# Binary files automatically placed in PREFIX/`uname -m`/
cd src
# Place in everything "in place" as siblings of src (i.e., create "include",
# "lib", "<mach>/lib", etc.).
# On x86_64 machine:
make install64 PREFIX=../..
# If you wish particular languages not to be built, add one or more of the
# following to the make command.
SKNOBS_C=0
SKNOBS_TCL=0
SKNOBS_PYTHON=0
SKNOBS_VERILOG=0
SKNOBS_PERL=0
